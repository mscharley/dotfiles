#!/usr/bin/env zsh

# Setup aliases for our project folders

#for f in $HOME/Code/*/; do
#  f=$(basename $f)
#  if `which $f > /dev/null`; then
#  else
#    $f() { cd $HOME/Code/$0/$1; }
#    compctl -W $HOME/Code/$f/ -/ $f
#  fi
#done
