# vim: ft=ruby:

# Fonts
cask 'font-fira-code'
cask 'font-symbols-only-nerd-font'
cask 'font-victor-mono'

# Neovim + dependencies
brew 'neovim'
brew 'ack'
brew 'bat'
brew 'fd'
brew 'fzf'

# CLI utilities
brew 'asdf'
brew 'git'
brew 'git-lfs'
brew 'jq'
brew 'kubernetes-cli'
brew 'most'
brew 'ripgrep'
brew 'starship'
brew 'yq'

# fetch
brew 'fastfetch'
brew 'hyfetch'

# AWS CLI (OSX only for now - session-manager-plugin)
brew 'awscli' if OS.mac?
cask 'session-manager-plugin' if OS.mac?

# OS X apps
brew 'coreutils' if OS.mac?
brew 'fish' if OS.mac?
brew 'gnu-sed' if OS.mac?
brew 'watch' if OS.mac?
brew 'xz' if OS.mac?
brew 'zsh' if OS.mac?
brew 'zsh-completions' if OS.mac?

cask 'alfred' if OS.mac?
cask 'cyberduck' if OS.mac?
cask 'docker-desktop' if OS.mac?
brew 'docker-credential-helper' if OS.mac?
cask 'kitty' if OS.mac?
